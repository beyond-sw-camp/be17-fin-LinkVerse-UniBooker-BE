server:
  port: 8084

spring:
  application:
    name: api-statistics

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/
    registerWithEureka: true
    fetchRegistry: true